import React from 'react';
import { Switch } from 'react-router-dom';

const Routes = (props) => {
  return <Switch></Switch>;
};

Routes.defaultProps = {
  routes: [{}],
};

Routes.propTypes = {};

export default Routes;
